name: Repair Osaka CSS formatting
on:
  push:
    branches: [main]

permissions:
  contents: write

jobs:
  repair:
    if: "!contains(github.event.head_commit.message, '[css-fix]')"
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Convert accidental literal newline escapes to real newlines
        run: |
          python3 - <<'PY'
          from pathlib import Path
          p = Path('index.html')
          s = p.read_text(encoding='utf-8')
          if '\\n/* SECTION STABILITY FIX */' in s:
              s = s.replace('\\n', '\n')
              p.write_text(s, encoding='utf-8')
          PY
      - name: Commit CSS repair
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add index.html
          git diff --cached --quiet || git commit -m "Repair CSS formatting [css-fix]"
          git push
